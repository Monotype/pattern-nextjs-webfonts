export default function Page() {
  return (
    <main>
      <h1>Next.js Web Fonts Pattern</h1>
      <p>
        Font loaded via <code>next/font/local</code> on <code>&lt;body&gt;</code>{' '}
        in <code>app/layout.tsx</code> — self-hosted from this app (not fetched
        from a third-party font CDN).
      </p>
    </main>
  )
}
